# Backend dev/test dependencies
# Install: pip install -r requirements-dev.txt

pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0   # async test client support for FastAPI
pytest-cov>=5.0.0,<6.0.0        # coverage reports
httpx>=0.27.0,<1.0.0             # async HTTP client used by FastAPI TestClient